# PostgreSQL credentials — used by the postgres service and both backend services
POSTGRES_USER=wled_seq
POSTGRES_PASSWORD=changeme

# URL the LED daemon uses to reach the API (no trailing slash)
# Change this if the API is not running on the default port
WLED_SEQ_API_URL=http://localhost:8080